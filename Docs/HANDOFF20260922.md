# 30 Acts of Kindness — handoff, 22 September 2026

Release **1 October 2026**. Nine days out.
Continues `HANDOFF20260921.md`.

Items 27 and 28 are built, tested end to end on staging with real texts, and
committed as `dea4943`. **Nothing was deployed to production.** The commit is
not pushed.

---

## What was proven

Three device tests against staging, all passed.

| | setup | result |
|---|---|---|
| **A** | account active (last act 20 Sep) | one **normal** reminder, `sent: 1`, `shut_off: 0` |
| **B** | completions from the 11th onward deleted | the **shutoff** text, `shut_off: 1`, reminders switched off, row recorded |
| **C** | `created_at` moved to now, no completions | the **normal** reminder again — grace period holds |

C is the one that matters most. A brand-new signup who hasn't logged an act yet
must NOT be switched off, and when the grace period applies the code correctly
falls through to the ordinary reminder rather than going silent. Getting the
shutoff text there would have been the failure that hurt real users.

Item 28's server side was proven separately and for free: the invoke summary's
`checked` fell from **6 to 3** — one slot per user instead of two. The app side
was confirmed on Gary's phone after the preview OTA (update `01a0c9e6`): a
single REMINDER TIME block, no SECOND REMINDER section.

`npm test`: **21 suites, 343 tests**, all passing.

---

## Two defects found in testing, both fixed

Neither could have been caught by the test suite, and that is the point.

### The shutoff row was being rejected

`SHUTOFF_SLOT` was 0. `reminder_sends` carries

```
CHECK (slot = ANY (ARRAY[1, 2]))
```

from when slots 1 and 2 were the only things recorded there. Every shutoff
insert was rejected.

The visible behaviour was fine — text sent, reminders disabled — so the test
"passed" while the idempotency guard did nothing at all. In production that
would have left one safeguard doing the work of two: if `disableReminders` ever
failed, the person would be texted every day with nothing to stop it.

**Fixed:** `SHUTOFF_SLOT = 1`, sharing the slot with the real reminder. That is
also better behaviour — if a reminder already went out today, the already-sent
check short-circuits before the inactivity check, so the shutoff waits until
tomorrow instead of landing as a second text the same day. `status` is what
tells the two apart (`shutoff_notice` vs `sent`).

Rejected the alternative — widening the constraint to allow 0 — because it means
a schema change on two projects nine days from launch, and staging and
production have drifted before (`confirm_phone_users`).

### The rejection was invisible

`recordSend` discarded its insert result. supabase-js **returns** errors rather
than throwing, so the failure never surfaced.

**Fixed:** it now destructures the error and `console.error`s it, with the
reason in a comment. A test asserts the check is there, so it cannot be quietly
removed.

**Backlog item 51 added**: `recordOptOut` and `disableReminders` still discard
their errors, three more call sites of the same bug. `disableReminders` is the
serious one — a silent failure there after the text has gone out means the
person keeps their schedule and gets texted again tomorrow.

---

## How half an hour got lost, and what to do differently

Worth recording, because it will happen again otherwise.

**My file writes were landing a version behind all morning.** The file on disk
would still hold the previous version after a write reported success, and my
view of the disk lagged too — so I twice read state that was not current and
reasoned from it. That produced two wrong theories about why the row was
missing, and a deploy that shipped the unfixed code while everyone believed
otherwise.

**What actually worked:** reading the file through Gary's own shell.

```powershell
Select-String -Path supabase\functions\send-reminders\index.ts -Pattern 'SHUTOFF_SLOT = '
```

**The rule for next time:** after any file is written and before it is deployed,
read it back through PowerShell. Do not trust a write that reported success, and
do not trust my view of the disk. `npm test` serves the same purpose for test
files — the test count is the tell (342 vs 343 told us exactly which version had
landed).

A second, smaller trap: a `supabase functions deploy` block in scrollback looks
identical to a fresh one. A deploy was read as having run when it had not.

The related lesson is the one item 13 already cost a day for: when a fix reasoned
from code does not work, stop and get evidence. Reproducing the failing insert
directly in SQL — which succeeded — is what proved the row shape was fine and
the deployed code was not.

---

## Staging data: restored and proven

Before testing, four snapshot tables and a fingerprint of every table the tests
could touch. Afterwards, the restore, then the same fingerprint query.

| table | rows | fingerprint |
|---|---|---|
| auth_users | 15 | `96fcfd02fdc0d9166612c62a580d6621` |
| completions | 234 | `b0263a766d39897b2d3173459a831377` |
| profiles | 14 | `2d521a2e366a08c0ffea056e7192e827` |
| reminder_sends | 271 | `201d734c170b5b76769a4df7e60ee3a1` |

All four identical before and after. The snapshot tables have been dropped.

The restore was deliberately additive in one direction and subtractive in the
other — re-insert completions missing against the snapshot, delete
`reminder_sends` rows not in it — with no `truncate`, because `completions` is
referenced by `completion_challenges`.

**This is the pattern to reuse.** A fingerprint turns "I think it's back" into
something provable, and it costs one query.

---

## Notes for the next round of testing

- `send-reminders` has **no single-user mode**. It walks every user and texts
  whoever matches, so a test invocation evaluates everyone. Today's window
  (09:30–noon CT) was chosen to miss the other testers' times: 09:00 Chicago and
  13:10 Phoenix (= 15:10 Chicago), each ±2 minutes.
- Staging has **no cron**, so nothing fires unless invoked by hand. That is a
  feature — it is what makes the above safe.
- The match window is ±2 minutes. Setting a slot three minutes out and invoking
  immediately misses it; wait until two minutes before the target.
- `reminder_sends` blocks a repeat for the same user, slot and day, so today's
  rows must be cleared between tests or the next one silently does nothing.
- The invoke needs staging's `REMINDERS_SECRET_KEY` — the secret API key named
  `reminders` on the staging project, **not** the masked string on Edge
  Functions → Secrets, which is display only.
- `Read-Host -AsSecureString` swallows a pasted value. `Get-Clipboard` works.

---

## Open

- **`dea4943` is not pushed.**
- **Production deploy of `send-reminders` has not happened** and is part of the
  1 October sequence. The Edge Function is not in the app — deploying it changes
  behaviour for every user instantly, so it goes out alongside the OTA, not
  before it.
- **Item 51** — the three remaining swallowed errors.
- Item 49 (Create Group's light palette) and the rest of the priority-1 list.

## Standing rules

- One step at a time, each with an explicit verification.
- No placeholder text in commands — it gets pasted literally.
- `||` does not work in the Supabase SQL editor; use `concat()`.
- Never publish to `--branch production` until Release This Version is pressed.
- Database dumps live in `Desktop\30acts-backups\`, outside the repo, because
  they contain real phone numbers.
- Watch what `git add -A` sweeps up.
- Never paste secrets into chat.
- **New:** read a file back through PowerShell before deploying it, and close
  the backlog spreadsheet before anything else writes to it.
